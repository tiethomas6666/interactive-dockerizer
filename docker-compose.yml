services:
  app:
    build: .
    ports:
      - "8080:8080"
  db:
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - db_data:/var/lib/mysql
  cache:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  db_data: